{{ formatIntl
        'email.invite.message'
        messages=messages
        name=(format '"{name}"' name=laundry.name)
        link='https://laundree.io'
        signup=(format 'https://laundree.io/{locale}/auth/sign-up' locale=locale)
        nl='' }}

{{ formatIntl
        'email.signature'
        messages=messages
        nl='' }}
