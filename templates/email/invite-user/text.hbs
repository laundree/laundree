{{ formatIntl
        'email.invite-user.message'
        messages=messages
        name=user.displayName
        laundry=(format '"{name}"' name=laundry.name)
        link="https://laundree.io"
        nl='' }}

{{ formatIntl
        'email.signature'
        messages=messages
        nl='' }}
