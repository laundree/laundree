{{message}}

{{ formatIntl
        'email.support.from'
        messages=messages
        userId=(format '"{userId}"' userId=userId)
        name=(format '"{name}"' name=name)
        email=(format '"{email}"' email=email) }}
