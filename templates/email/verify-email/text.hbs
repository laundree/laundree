{{ formatIntl
        'email.verify-email.message'
        messages=messages
        name=user.displayName
        email=(format '"{email}"' email=email)
        link=(format
                'https://laundree.io/auth/verify?user={userId}&email={email}&token={token}'
                userId=user.id
                email=emailEncoded
                token=token)
        nl='' }}

{{ formatIntl
        'email.signature'
        messages=messages
        nl='' }}
