web:
  port: 3000

redis:
  host: 'localhost'
  port: 6379

mongo:
  url: 'mongodb://localhost/laundree'

facebook:
  appId: '1044245858966868'
  appSecret: '394f0ef62fd8d104e110ba898afeeb4a'
  callbackUrl: 'http://localhost:3000/auth/facebook/callback'

google:
  clientId: '1098336985149-el1jqcb5j7d3oldav4csv0nh976pvsch.apps.googleusercontent.com'
  clientSecret: '0uWixpXV9BwwXTTeZYm0fvls'
  callbackUrl: 'http://localhost:3000/auth/google/callback'

session:
  secret: 'secret1234'

security:
  password:
    saltRounds: 10

swagger:

  fittingsDirs: [ api/fittings ]
  defaultPipe: null
  swaggerControllerPipe: swagger_controllers  # defines the standard processing pipe for controllers

  # values defined in the bagpipes key are the bagpipes pipes and fittings definitions
  # (see https://github.com/apigee-127/bagpipes)
  bagpipes:

    _router:
      name: swagger_router
      mockMode: false
      mockControllersDirs: [ api/mocks ]
      controllersDirs: [ api/controllers ]

    _swagger_validate:
      name: swagger_validator
      validateResponse: true

    # pipe for all swagger-node controllers
    swagger_controllers:
      - onError: json_error_handler
      - cors
      - swagger_security
      - swagger_params_parser
      - _swagger_validate
      - express_compatibility
      - _router

    # pipe to serve swagger (endpoint is in swagger.yaml)
    swagger_raw:
      name: swagger_raw
